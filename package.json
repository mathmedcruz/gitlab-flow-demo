{
  "name": "gitlab-flow-demo",
  "version": "0.1.0",
  "description": "Projeto didático para demonstrar GitLab Flow com branches de ambiente (dev/staging/production) e GitHub Actions.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
